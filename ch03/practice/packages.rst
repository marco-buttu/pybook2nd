Per installare il package Sphinx nell'ambiente virtuale ``myproject``,
dobbiamo prima di tutto attivare l'ambiente virtuale. Fatto ciò, usiamo
pip. 

Con pyenv::

    $ pyenv shell myproject
    $ pip install sphinx

Con conda::

    $ source activate myproject
    $ pip install sphinx
